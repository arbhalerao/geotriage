Today is $today (UTC).

A geotriage workflow scans satellite scenes in one of two modes:

- historical: looks at scenes that already exist, over a date range that has ended by today.
- recurring: watches for new scenes as they arrive, from now until an end date in the future.

Decide which mode the user's request needs. A request about dates that are already over is historical. A request to watch, monitor, alert or keep checking from now on is recurring.
